import { apiGet } from '@/lib/api';
export const dynamic = 'force-dynamic';
export const metadata = { title: 'سياسة الخصوصية — هنا قنا' };
export default async function PrivacyPage() { const settings = await apiGet<{ privacyPolicy: string }>('/api/settings', { revalidate: 60 }).catch(() => ({ privacyPolicy: '' })); return <article className="surface legalPage"><span className="eyebrow">هنا قنا</span><h1 className="pageTitle">سياسة الخصوصية</h1>{settings.privacyPolicy ? <p style={{ whiteSpace: 'pre-wrap' }}>{settings.privacyPolicy}</p> : <><p>نستخدم بيانات الحساب والمنطقة والاهتمامات لتحسين الترشيحات وتشغيل الخدمات داخل التطبيق. لا نبيع بيانات المستخدمين.</p><h2>البيانات التي نجمعها</h2><p>الاسم ورقم الهاتف والبريد الاختياري والمناطق والاهتمامات والمساهمات التي يختار المستخدم إرسالها.</p><h2>التحكم والحذف</h2><p>يمكنك تعديل إعدادات الخصوصية أو حذف الحساب من داخل التطبيق.</p></>}</article>; }
