import Link from 'next/link'; import { redirect } from 'next/navigation'; import { apiGet } from '@/lib/api'; import { hasAdminSession } from '@/lib/admin-session'; import { moderateOffer, moderateService } from '../actions';
export const dynamic = 'force-dynamic'; type Service = { id: string; name: string; description?: string | null; price?: string | number | null; priceNote?: string | null; status: string; provider: { name: string } }; type Offer = { id: string; title: string; description?: string | null; startsAt: string; endsAt: string; status: string; provider: { name: string } };
export default async function ServicesAdminPage() { if (!await hasAdminSession()) redirect('/admin/login'); const [services, offers] = await Promise.all([apiGet<Service[]>('/api/admin/services', { admin: true }).catch(() => []), apiGet<Offer[]>('/api/admin/offers', { admin: true }).catch(() => [])]); return <section><div className="sectionHead"><div><span className="eyebrow">محتوى الأنشطة</span><h1 className="pageTitle">الخدمات والعروض</h1></div><Link href="/admin">العودة للوحة</Link></div><p className="pageLead">راجع الخدمات والباقات والعروض التي يضيفها أصحاب الأنشطة قبل نشرها.</p><section className="section surface table"><div className="sectionHead" style={{ padding: '18px 18px 0' }}><h2>الخدمات</h2><span className="badge">{services.length}</span></div><table><thead><tr><th>الخدمة</th><th>النشاط</th><th>السعر</th><th>الحالة</th><th>قرار</th></tr></thead><tbody>{services.map((item) => <tr key={item.id}><td>{item.name}<small>{item.description}</small></td><td>{item.provider.name}</td><td>{item.price ?? '—'} {item.priceNote ?? ''}</td><td><span className="badge">{item.status}</span></td><td>{item.status === 'PENDING' && <div className="actionRow"><form action={moderateService}><input type="hidden" name="id" value={item.id}/><input type="hidden" name="status" value="APPROVED"/><button className="approveButton" type="submit">اعتماد</button></form><form action={moderateService}><input type="hidden" name="id" value={item.id}/><input type="hidden" name="status" value="REJECTED"/><button className="rejectButton" type="submit">رفض</button></form></div>}</td></tr>)}</tbody></table></section><section className="section surface table"><div className="sectionHead" style={{ padding: '18px 18px 0' }}><h2>العروض</h2><span className="badge">{offers.length}</span></div><table><thead><tr><th>العرض</th><th>النشاط</th><th>الفترة</th><th>الحالة</th><th>قرار</th></tr></thead><tbody>{offers.map((item) => <tr key={item.id}><td>{item.title}<small>{item.description}</small></td><td>{item.provider.name}</td><td>{new Date(item.startsAt).toLocaleDateString('ar-EG')} — {new Date(item.endsAt).toLocaleDateString('ar-EG')}</td><td><span className="badge">{item.status}</span></td><td>{item.status === 'PENDING' && <div className="actionRow"><form action={moderateOffer}><input type="hidden" name="id" value={item.id}/><input type="hidden" name="status" value="APPROVED"/><button className="approveButton" type="submit">اعتماد</button></form><form action={moderateOffer}><input type="hidden" name="id" value={item.id}/><input type="hidden" name="status" value="REJECTED"/><button className="rejectButton" type="submit">رفض</button></form></div>}</td></tr>)}</tbody></table></section></section>; }
