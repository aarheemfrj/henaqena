import Link from 'next/link';
import { redirect } from 'next/navigation';
import { apiGet } from '@/lib/api';
import { hasAdminSession } from '@/lib/admin-session';
import { archivePriceAdmin, createPrice, moderatePrice, updatePriceAdmin } from '../actions';

export const dynamic = 'force-dynamic';
type Price = { id: string; name: string; category?: string | null; minPrice: string | number; maxPrice: string | number; unit?: string | null; status: string; validUntil?: string | null; confidenceScore?: number; sourceType?: string; sourceNote?: string | null; archivedAt?: string | null; outlier?: boolean; outlierRatio?: number | null; outlierReason?: string | null };

export default async function PricesAdminPage({ searchParams }: { searchParams?: Promise<{ outliersOnly?: string }> }) {
  if (!await hasAdminSession()) redirect('/admin/login');
  const filters = await searchParams;
  const outliersOnly = filters?.outliersOnly === 'true';
  const prices = await apiGet<Price[]>(`/api/admin/prices${outliersOnly ? '?outliersOnly=true' : ''}`, { admin: true }).catch(() => []);
  return <section><div className="sectionHead"><div><span className="eyebrow">دليل الأسعار</span><h1 className="pageTitle">إدارة «بكام؟»</h1></div><Link href="/admin">العودة للوحة</Link></div><p className="pageLead">أضف السعر المرجعي بعد المراجعة. التطبيق يعرض النطاق السعري بدل رقم مضلل.</p><form action={createPrice} className="surface formGrid"><label>اسم الخدمة أو المنتج<input name="name" required /></label><label>الفئة<input name="category" /></label><label>من<input name="minPrice" type="number" min="0.01" step="0.01" required /></label><label>إلى<input name="maxPrice" type="number" min="0.01" step="0.01" required /></label><label>الوحدة<input name="unit" placeholder="للوحدة" /></label><label>المصدر<input name="sourceNote" placeholder="رصد مجتمعي أو مصدر رسمي" /></label><button className="primaryButton" type="submit">نشر السعر</button></form><section className="section surface table"><div className="sectionHead" style={{ padding: '18px 18px 0' }}><div><h2>الأسعار المسجلة</h2><span className="badge">{prices.length}</span></div><Link className="secondaryButton" href={outliersOnly ? '/admin/prices' : '/admin/prices?outliersOnly=true'}>{outliersOnly ? 'عرض كل الأسعار' : 'مراجعة الأسعار الشاذة'}</Link></div><table><thead><tr><th>التعديل</th><th>النطاق</th><th>الحالة</th><th>إشارة المراجعة</th><th>إجراء</th></tr></thead><tbody>{prices.map((price) => <tr key={price.id}><td><form action={updatePriceAdmin} className="compactForm"><input type="hidden" name="id" value={price.id} /><input name="name" defaultValue={price.name} required /><input name="category" defaultValue={price.category ?? ''} placeholder="الفئة" /><input name="sourceNote" defaultValue={price.sourceNote ?? ''} placeholder="ملاحظة المصدر" /><select name="sourceType" defaultValue={price.sourceType ?? 'COMMUNITY'}><option value="COMMUNITY">مجتمعي</option><option value="ADMIN">إدارة</option><option value="OFFICIAL">رسمي</option><option value="PROVIDER">مقدم خدمة</option></select><input name="validUntil" type="date" defaultValue={price.validUntil ? price.validUntil.slice(0, 10) : ''} /><input name="confidenceScore" type="number" min="0" max="100" defaultValue={price.confidenceScore ?? 50} /><button className="secondaryButton" type="submit">حفظ التعديل</button></form></td><td><form action={updatePriceAdmin} className="compactForm"><input type="hidden" name="id" value={price.id} /><input type="hidden" name="name" value={price.name} /><input type="hidden" name="category" value={price.category ?? ''} /><input type="hidden" name="unit" value={price.unit ?? ''} /><input type="hidden" name="sourceNote" value={price.sourceNote ?? ''} /><input type="hidden" name="sourceType" value={price.sourceType ?? 'COMMUNITY'} /><input type="hidden" name="validUntil" value={price.validUntil ? price.validUntil.slice(0, 10) : ''} /><input type="hidden" name="confidenceScore" value={price.confidenceScore ?? 50} /><input name="minPrice" type="number" min="0.01" step="0.01" defaultValue={price.minPrice} required /><input name="maxPrice" type="number" min="0.01" step="0.01" defaultValue={price.maxPrice} required /><button className="secondaryButton" type="submit">حفظ النطاق</button></form><small>{price.unit ?? 'جنيه'}</small></td><td><span className="badge">{price.status}</span></td><td>{price.outlier ? <span className="badge warningBadge" title={price.outlierReason ?? undefined}>شاذ ({price.outlierRatio}×)</span> : <span className="muted">طبيعي</span>}</td><td><div className="actionRow">{price.status === 'PENDING' && <><form action={moderatePrice}><input type="hidden" name="id" value={price.id} /><input type="hidden" name="status" value="APPROVED" /><button className="approveButton" type="submit">اعتماد</button></form><form action={moderatePrice}><input type="hidden" name="id" value={price.id} /><input type="hidden" name="status" value="REJECTED" /><button className="rejectButton" type="submit">رفض</button></form></> }<form action={archivePriceAdmin}><input type="hidden" name="id" value={price.id} /><input type="hidden" name="archived" value={price.archivedAt ? 'false' : 'true'} /><input type="hidden" name="reason" value="مراجعة إدارية" /><button className="secondaryButton" type="submit">{price.archivedAt ? 'استرجاع' : 'أرشفة'}</button></form></div></td></tr>)}</tbody></table></section></section>;
}
