import Link from 'next/link'; import { redirect } from 'next/navigation'; import { apiGet } from '@/lib/api'; import { hasAdminSession } from '@/lib/admin-session'; import { moderateReport } from '../actions';
export const dynamic = 'force-dynamic'; type Report = { id: string; kind: string; name: string; phone?: string | null; note?: string | null; status: string; provider?: { name: string } | null };
export default async function ReportsAdminPage() { if (!await hasAdminSession()) redirect('/admin/login'); const items = await apiGet<Report[]>('/api/admin/provider-reports', { admin: true }).catch(() => []); return <section><div className="sectionHead"><div><span className="eyebrow">المراجعة</span><h1 className="pageTitle">البلاغات</h1></div><Link href="/admin">العودة للوحة</Link></div><p className="pageLead">تابع بلاغات المستخدمين عن الأنشطة والبيانات غير الصحيحة.</p><section className="section surface table"><table><thead><tr><th>النوع</th><th>الاسم</th><th>النشاط</th><th>الملاحظة</th><th>الحالة</th><th>قرار</th></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td>{item.kind}</td><td>{item.name}</td><td>{item.provider?.name ?? 'نشاط غير موجود'}</td><td>{item.note ?? '—'}</td><td><span className="badge">{item.status}</span></td><td>{item.status === 'PENDING' && <div className="actionRow"><form action={moderateReport}><input type="hidden" name="id" value={item.id}/><input type="hidden" name="status" value="APPROVED"/><button className="approveButton" type="submit">تمت المعالجة</button></form><form action={moderateReport}><input type="hidden" name="id" value={item.id}/><input type="hidden" name="status" value="REJECTED"/><button className="rejectButton" type="submit">غير صحيح</button></form></div>}</td></tr>)}</tbody></table></section></section>; }
